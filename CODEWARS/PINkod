Банкоматы допускают использование 4- или 6-значных ПИН-кодов, а ПИН-коды не могут содержать ничего, кроме ровно 4 или ровно 6 цифр.

Если функции передается действительная строка ПИН-кода, верните true, иначе верните false.

function pin (d) {
 
if ((d.length == 4 || d.length == 6)   &&  (typeof d === 'number')) 
{
return true;
}
else {return false}

}


console.log(pin(33335))
